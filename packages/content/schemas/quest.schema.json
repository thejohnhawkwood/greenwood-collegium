{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://greenwood.local/schemas/quest.json",
  "title": "Greenwood quest template",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "title",
    "introNarration",
    "reminderNarration",
    "experienceReward",
    "objectives"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "introNarration": {
      "type": "string",
      "minLength": 1
    },
    "reminderNarration": {
      "type": "string",
      "minLength": 1
    },
    "experienceReward": {
      "type": "integer",
      "minimum": 1
    },
    "itemRewardTemplateId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "outcomes": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "completionNarration"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "completionNarration": {
            "type": "string",
            "minLength": 1
          },
          "itemRewardTemplateId": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          }
        }
      }
    },
    "objectives": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "kind", "label"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "kind": {
            "enum": ["look", "say", "take", "visit", "examine", "talk", "defeat", "cast", "equip"]
          },
          "label": {
            "type": "string",
            "minLength": 1
          },
          "itemTemplateId": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "roomId": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "targetId": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "requires": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "pattern": "^[a-z][a-z0-9-]*$"
            }
          },
          "outcome": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          }
        },
        "allOf": [
          {
            "if": {
              "properties": {
                "kind": {
                  "enum": ["examine", "talk", "defeat", "cast"]
                }
              }
            },
            "then": {
              "required": ["targetId"]
            }
          },
          {
            "if": {
              "properties": {
                "kind": {
                  "enum": ["take", "equip"]
                }
              }
            },
            "then": {
              "required": ["itemTemplateId"]
            }
          },
          {
            "if": {
              "properties": {
                "kind": {
                  "const": "visit"
                }
              }
            },
            "then": {
              "required": ["roomId"]
            }
          }
        ]
      }
    },
    "giverNpcId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "requiresQuestIds": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$"
      }
    },
    "completionNarration": {
      "type": "string",
      "minLength": 1
    }
  }
}
